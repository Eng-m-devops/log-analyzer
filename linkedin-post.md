# LinkedIn Post — Day 2/30

🚀 **Day 2/30 — Log Analyzer**

في DevOps، السجلات ليست مجرد نصوص طويلة نقرأها عند حدوث مشكلة؛ إنها مصدر مهم لفهم ما يحدث داخل النظام.

في مشروع اليوم الثاني بنيت أداة **Log Analyzer** باستخدام **Bash** وأدوات Linux القياسية لتحويل access logs الخام إلى معلومات قابلة للفهم والاستخدام.

السكربت يقوم بتحليل:

- إجمالي عدد الطلبات.
- عدد أخطاء HTTP من نوع **4xx**.
- عدد أخطاء HTTP من نوع **5xx**.
- إجمالي الأخطاء.
- أكثر عناوين IP تكرارًا.
- أكثر URLs طلبًا.
- أكثر الروابط التي تسبب أخطاء 4xx.
- أكثر الروابط التي تسبب أخطاء 5xx.

في بيانات الاختبار، حلل السكربت **20 طلبًا**، واكتشف **5 أخطاء 4xx** و**4 أخطاء 5xx**. كما أظهر أن `/missing` هو أكثر URL تسببًا في أخطاء 4xx، بينما ظهرت `/api/unknown` و`/api/orders` ضمن أخطاء 5xx.

الأوامر التي طبقتها في المشروع:

`awk` لاستخراج الحقول وتحليل status codes

`sort` لترتيب النتائج

`uniq -c` لحساب التكرارات

`wc -l` لحساب عدد الطلبات

`head` لعرض أعلى النتائج

`grep` وLinux pipelines لمعالجة البيانات

كما أضفت إلى السكربت:

- دعم تمرير ملف log مخصص من سطر الأوامر.
- خيار `--help`.
- التحقق من وجود الملف وقابليته للقراءة وعدم فراغه.
- التحقق من أن الملف يشبه access log صالحًا.
- Exit Codes واضحة للتعامل مع الأخطاء.

أهم درس تعلمته اليوم هو أن أدوات Linux الصغيرة تصبح قوية جدًا عند ربطها معًا في Pipeline واحدة:

```bash
awk → sort → uniq -c → sort -nr → head
```

هذه الخطوة نقلتني من مجرد تشغيل أوامر منفصلة إلى بناء أداة CLI عملية يمكن تطويرها لاحقًا لتعمل مع monitoring وalerting وCI/CD.

**Day 2/30 ✅**

Next: **Day 3 — New DevOps Project** 🚀

#DevOps #Linux #Bash #ShellScripting #LogAnalysis #Monitoring #Troubleshooting #Automation #DevOpsJourney #100DaysOfCode
